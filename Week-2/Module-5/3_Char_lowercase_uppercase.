#include <stdio.h>

int main() {
    char A;
    scanf("%c", &A);
    
    if (A >= 'a' && A <= 'z') {
        // letter is lowercase, so convert it to uppercase and print
        printf("%c", A-32);
    } else if (A >= 'A' && A <= 'Z') {
        // letter is uppercase, so convert it to lowercase and print
        printf("%c", A+32);
    }
    
    return 0;
}
